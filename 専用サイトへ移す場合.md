# （参考・未実施）専用サイトへ移す場合の手順

> **現在は採用していません。**
> このアプリは https://omake-app.web.app/ で運用中です。通常のデプロイは [デプロイ手順.md](デプロイ手順.md) を見てください。
>
> このファイルは「将来、デフォルトサイトを空けて専用URLに移したくなったとき」のための控えです。

---

## 背景

`omake-app.web.app` は omake-app プロジェクトの**デフォルトサイト**で、いまこのアプリが使っています。
他のアプリ（CONV-KEIH-APP など）は、それぞれ専用サイトを持っています。

| 現状 | |
|---|---|
| omake-app.web.app | 出張旅費精算アプリ（このアプリ・デフォルトサイト） |
| keihi-kanri.web.app | 経費管理アプリ（CONV-KEIH-APP） |

デフォルトサイトを他の用途に空けたくなったら、以下の手順で専用サイトへ移せます。

---

## 手順

### 1. 専用サイトを作成

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && firebase hosting:sites:create shucchou-ryohi-seisan
```

### 2. firebase.json のサイト名を書き換え

`"site": "omake-app"` を `"site": "shucchou-ryohi-seisan"` に変更します。

### 3. デプロイ

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && firebase deploy --only hosting
```

これで **https://shucchou-ryohi-seisan.web.app/** で公開されます。

---

## 移行時の注意

- **URLが変わります。** 社内に配布済みのURLや、スマホのホーム画面に追加した PWA アイコンは、すべて貼り替えが必要です。
- 旧URL（omake-app.web.app）を残したい場合は、デフォルトサイト側に新URLへのリダイレクトを置くか、しばらく両方へデプロイしてください。
- 移行が済むまでは、旧URLを見ている人が古い画面のままになる点に注意してください。
