# 出張旅費精算アプリ デプロイ手順

## 公開URL

**https://shucchou-ryohi-seisan.web.app/**

| 項目 | 値 |
|---|---|
| Firebase プロジェクト | `new-check-137f9` |
| Hosting サイト | `shucchou-ryohi-seisan` |
| GitHub | https://github.com/fkt78/FKT-SRK-APP |
| フォルダ | `~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP` |

サイトは作成済みです。初回セットアップは不要です。

> **注意：プロジェクトは `omake-app` ではありません。**
> サイト名から `omake-app` プロジェクトだと勘違いしやすいですが、
> `shucchou-ryohi-seisan` は **`new-check-137f9`** プロジェクトに属しています。

---

## デプロイ実行

ターミナル.app を開いて、以下をまとめて実行します。

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && git add -A && git commit -m "更新" && git push && firebase deploy --only hosting --project new-check-137f9
```

`デプロイ.sh` をダブルクリックしても、GitHub へのプッシュと Firebase デプロイをまとめて実行できます。

### 個別に実行する場合

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP

# 1. GitHub にプッシュ
git add -A
git commit -m "更新内容をここに書く"
git push

# 2. Firebase にデプロイ
firebase deploy --only hosting --project new-check-137f9
```

`--project new-check-137f9` は付けておくことを推奨します。firebase CLI のアクティブ
プロジェクトは他フォルダでの作業につられて変わることがあり、付けておけば誤爆しません。

---

## デプロイ先の確認

不安なときは、デプロイ前に向き先を確認できます。

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && firebase use
```

`new-check-137f9` と表示されればOKです。違う場合は次で切り替えます。

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && firebase use new-check-137f9
```

---

## アクセスURL一覧

| URL | 説明 |
|-----|------|
| https://shucchou-ryohi-seisan.web.app/ | **メイン**（出張旅費精算アプリ） |
| https://shucchou-ryohi-seisan.web.app/出張手当管理アプリ.html | 旧HTML。トップへ301転送 |
| https://omake-app.web.app/ | 旧URL。上のメインへ301転送（[omake-app-redirect/](omake-app-redirect/README.md) で管理） |

---

## デプロイされるファイル

`public: "."` でフォルダ全体が対象になるため、`firebase.json` の `ignore` で以下を除外しています。

- `.git/**` … リポジトリ全体が公開されるのを防ぐ
- `*.md` / `*.sh` … 手順書とスクリプトは公開不要
- `omake-app-redirect/**` … 別プロジェクト用の設定

実際に公開されるのは **index.html / manifest.json / sw.js / icon.svg の4点**です。
デプロイ時に `found 4 files` と出れば正常です。ここが極端に多い場合は `ignore` が
効いていないので確認してください。

---

## 同じ Firebase 内の他アプリ

混同しないよう注意してください。

| URL | アプリ | プロジェクト |
|---|---|---|
| shucchou-ryohi-seisan.web.app | 出張旅費精算（このアプリ） | new-check-137f9 |
| keihi-kanri.web.app | 経費管理（CONV-KEIH-APP） | omake-app |
