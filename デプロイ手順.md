# 出張旅費精算アプリ デプロイ手順

## 公開URL

**https://omake-app.web.app/**

- Firebase プロジェクト: `omake-app`
- Hosting サイト: `omake-app`（プロジェクトのデフォルトサイト）
- GitHub: https://github.com/fkt78/FKT-SRK-APP

サイトは作成済みなので、初回セットアップは不要です。

---

## このフォルダの場所

```
~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP
```

---

## デプロイ実行

ターミナル.app を開いて、以下をまとめて実行します。

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && git add -A && git commit -m "更新" && git push && firebase deploy --only hosting
```

`デプロイ.sh` をダブルクリックしても、GitHub へのプッシュと Firebase デプロイをまとめて実行できます。

### 個別に実行する場合

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP

# 1. GitHub にプッシュ
git add -A
git commit -m "更新内容をここに書く"
git push

# 2. Firebase にデプロイ
firebase deploy --only hosting
```

---

## デプロイ前の確認

デプロイ先を間違えないよう、初回や久しぶりのときは確認しておくと安全です。

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && firebase use
```

`omake-app` と表示されればOKです。別のプロジェクト名が出た場合は、次を実行して切り替えます。

```bash
cd ~/（有）吹田総業/CONV-APP/CONV-SYUTTYOU-APP && firebase use omake-app
```

---

## アクセスURL一覧

| URL | 説明 |
|-----|------|
| https://omake-app.web.app/ | メイン（出張旅費精算アプリ） |
| https://omake-app.web.app/出張手当管理アプリ.html | 旧URL。トップへ301リダイレクトされます |

---

## 注意

- `omake-app.web.app` は omake-app プロジェクトの**デフォルトサイト**です。このアプリが占有しています。
- 同じ omake-app プロジェクト内の `keihi-kanri.web.app` は**経費管理アプリ（CONV-KEIH-APP）**です。別アプリなので混同しないよう注意してください。
- 将来このアプリを専用サイトへ移したくなった場合は `専用サイトへ移す場合.md` を参照してください。
